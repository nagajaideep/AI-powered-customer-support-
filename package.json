{
  "dependencies": {
    "@types/express": "^5.0.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "openai": "^6.20.0"
  },
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix backend\" \"npm:dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}